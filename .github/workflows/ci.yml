name: ci

on:
  push:
    tags:
      - "*"
      - "!v*"

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.98.1
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@v2
        with:
          tool: nextest
          checksum: false
      - name: Cargo fmt
        run: cargo fmt --all --check
      - name: Cargo check
        run: cargo check --all-targets --all-features
      - name: Cargo missing docs
        run: cargo rustc --lib --all-features -- -D missing-docs
      - name: Cargo clippy
        run: cargo clippy --all-targets --all-features -- -D warnings
      - name: Cargo doctest
        run: cargo test --doc --all-features
      - name: Cargo nextest all-targets
        run: cargo nextest run --all-targets --all-features
      - name: Cargo nextest without default features
        run: cargo nextest run --all-targets --no-default-features
      - name: Cargo clippy without default features
        run: cargo clippy --all-targets --no-default-features -- -D warnings
      - name: Cargo doctest without default features
        run: cargo test --doc --no-default-features
      - name: PowerShell runtime regression
        run: cargo test --all-features generated_profile_loads_literal_paths_without_changing_user_variables -- --ignored
      - name: Cargo doc
        run: cargo doc --no-deps --all-features
      - name: Cargo publish dry run
        run: cargo publish --dry-run
